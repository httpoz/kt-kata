management:
  metrics:
    jvm: true
    tags:
      application: kt_kata
  endpoint:
    health.probes.enabled: true
  endpoints:
    enabled-by-default: true
    web:
      exposure.include: 'health,info,prometheus'
      base-path: '/'
